{% extends 'base.html.twig' %}

{% block title %}Edit UserVote{% endblock %}

{% block body %}
    <div class="bg-secondary mb-2 px-2">
        <a class="text-white" href="{{ path('app_user_vote_index') }}">Vote Listing</a>
        <span class="text-white">&gt; Edit</span>
    </div>

    <div class="row text-center mx-5">
        <div class="col-md-4 mx-auto">
            <div class="card border-dark">
                <div class="card-head">
                    <h1>Edit User Vote</h1>
                </div>
                <div class="card-body">
                    {{ include('user_vote/_form.html.twig', {'button_label': 'Update'}) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
